

import 'package:flutter/material.dart';

class GridDetailView extends StatelessWidget {
  const GridDetailView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('GRID VIEW DETAIL PAGE'),
      ),
      body: const Text('DETAIL GRID VIEW GOES HERE'),
    );
  }

}